# RubyGems Versions Large

Staged recipe for `rubygems_versions_large`.

- Source: https://rubygems.org/
- Local raw payload: `${DATA_DIR:-.data}/downloads/rubygems_versions_large/rubygems_versions.json`
- This recipe is staged only. Promote it to `datasets/` after a fresh user-run download and passing `build.sh` plus `verify.sh`.
