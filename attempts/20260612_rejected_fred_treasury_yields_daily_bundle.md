# fred_treasury_yields_daily_bundle

Status: rejected

Reason:
- the bundle was homogeneous, but still too small to justify acceptance under the current floor

Observed fresh build:
- sample files: `3`
- series:
  - `DGS2`
  - `DGS10`
  - `DGS30`
- total values: `7,503`
- total bytes: `30,012`

Conclusion:
- do not accept the current three-maturity treasury-yields bundle as a standalone dataset
- it should only be reconsidered if widened materially while preserving homogeneity
