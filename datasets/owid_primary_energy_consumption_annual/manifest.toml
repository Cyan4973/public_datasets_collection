manifest_version = 1
dataset_id = "owid_primary_energy_consumption_annual"
name = "OWID Primary Energy Consumption Annual"
description = "Our World in Data annual primary energy consumption values from the OWID primary energy consumption data CSV, emitted as one sample per selected country."
homepage = "https://ourworldindata.org/co2-and-greenhouse-gas-emissions"

[license]
name = "CC BY 4.0"
spdx = "CC-BY-4.0"
url = "https://github.com/owid/co2-data"
citation = "Our World in Data CO2 Data, primary_energy_consumption column."
notes = "Confirm downstream attribution handling during review."

[safety]
contains_sensitive_data = false
contains_personal_data = false
provenance_notes = "Official Our World in Data CO2 CSV only."
usage_notes = "Public country-level emissions and energy data suitable for reproducible numeric dataset collection."

[[origins]]
id = "owid_primary_energy_consumption_github_raw"
name = "OWID CO2 GitHub Raw"
url = "https://raw.githubusercontent.com/owid/co2-data/"
type = "file_download"
notes = "Public raw CSV files from the OWID CO2 data repository."

[[resources]]
id = "owid_primary_energy_consumption_csv"
origin = "owid_primary_energy_consumption_github_raw"
url = "https://raw.githubusercontent.com/owid/co2-data/master/owid-co2-data.csv"
type = "csv"
version = "master"
access = "Single fixed raw CSV URL."
size_bytes = 0
sha256 = ""
local_path = "downloads/owid_primary_energy_consumption_annual/"
notes = "Annual country rows with multiple emissions and energy columns."

[local_paths]
downloads = "downloads/owid_primary_energy_consumption_annual/"
extracted = "extracted/owid_primary_energy_consumption_annual/"
filtered = "filtered/owid_primary_energy_consumption_annual/"
index = "index/owid_primary_energy_consumption_annual/"
samples = "samples/owid_primary_energy_consumption_annual/"

[processing]
download_script = "download.sh"
build_script = "build.sh"
verify_script = "verify.sh"
sample_index_path = "index/owid_primary_energy_consumption_annual/samples.jsonl"
summary = "Download the OWID CO2 CSV, retain parseable annual primary energy consumption rows for selected countries, and emit numeric arrays plus a machine-readable sample index."
deterministic_notes = "Fixed input URL, fixed country list, fixed target column, download-time CSV validation, filtered blank and malformed rows only, sorted retained rows by year per country, durable logs under logs/owid_primary_energy_consumption_annual/, and generated sample index at index/owid_primary_energy_consumption_annual/samples.jsonl."

[[series]]
id = "primary_energy_consumption_f32"
description = "Annual OWID primary energy consumption values."
semantic_meaning = "Float32 annual primary energy consumption values, one sample per selected country."
source_resources = ["owid_primary_energy_consumption_csv"]
filtered_input_path = "filtered/owid_primary_energy_consumption_annual/country_stats.tsv"
filtering = "Keep rows with selected ISO3 country codes, parseable years, and parseable primary energy consumption values."
missing_value_policy = "Filter rows with blank or malformed year or primary energy consumption values."
conversion = "Sort retained rows by ascending year within each country and write IEEE-754 little-endian float32 arrays."
numeric_kind = "float"
bit_width = 32
endianness = "little"
sample_format = "raw homogeneous numeric array"
sample_count = 0
total_size_bytes = 0
output_path = "samples/owid_primary_energy_consumption_annual/primary_energy_consumption_f32/"

