# CRAN Packages

Pinned CRAN package database snapshot with package-level structural counts.

Selected series:
- `cran_archived_flag`
- `cran_version_count`
- `cran_release_count`
- `cran_import_count`
- `cran_suggest_count`

Missing-value policy: preserves zero counts and filters no rows.
