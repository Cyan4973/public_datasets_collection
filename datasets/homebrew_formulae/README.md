# Homebrew Formulae

Pinned Homebrew formula catalog with native package metadata counts.

Selected series:
- `homebrew_revision`
- `homebrew_build_dependency_count`
- `homebrew_dependency_count`
- `homebrew_versioned_formula_count`
- `homebrew_executable_count`

Missing-value policy: preserves zero counts and filters no rows.
