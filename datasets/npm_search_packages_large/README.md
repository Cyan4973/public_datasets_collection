# npm Search Packages Large

Staged recipe for `npm_search_packages_large`.

- Source: https://registry.npmjs.org/-/v1/search
- Local raw payload: `${DATA_DIR:-.data}/downloads/npm_search_packages_large/npm_search_packages_large.json`
- This recipe is staged only. Promote it to `datasets/` after a fresh user-run download and passing `build.sh` plus `verify.sh`.
